import { Typography } from '@mui/material';

const Campaigns = () => {
  return (
    <Typography variant="h1" component="h1">
      Campaigns Page
    </Typography>
  );
};

export default Campaigns;
